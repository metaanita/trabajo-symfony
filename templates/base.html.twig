!DO<CTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mi trabajo-symfony{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    {% block stylesheets %}{% endblock %}
    <style>
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Cambio de tipo de letra */
            background-color: #f8f9fa; /* Cambio de color de fondo */
        }
        .content {
            flex: 1;
        }
        .footer {
            flex-shrink: 0;
            background-color: #343a40; /* Cambio de color de fondo del footer */
            color: #ffffff; /* Cambio de color del texto del footer */
        }
        .navbar {
            background-color: #007bff; /* Cambio de color de fondo de la barra de navegación */
        }
        .navbar-brand {
            color: #ffffff; /* Cambio de color del texto de la marca de la barra de navegación */
        }
        .nav-link {
            color: #ffffff; /* Cambio de color del texto de los enlaces de la barra de navegación */
        }
        .nav-link:hover {
            color: #0056b3; /* Cambio de color al pasar el mouse sobre los enlaces de la barra de navegación */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">Mi Web</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_home') }}">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Registro</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Iniciar Sesión</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4 content">
        {% block body %}{% endblock %}
    </div>

    <footer class="footer mt-auto py-3 bg-light">
        <div class="container">
            <span class="text-muted">© {{ "now"|date("Y") }} Mi página Web: Todos los derechos reservados.</span>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
